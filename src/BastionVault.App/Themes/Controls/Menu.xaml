<!--
    Menus, context menus and menu items. Every item reserves a 28 px glyph column and a
    right-aligned shortcut column, so a menu reads as a table of "picture, verb, keys".
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============ Separator inside a menu: the etched seam ============ -->
    <Style x:Key="Menu.Separator" TargetType="Separator">
        <Setter Property="Height" Value="2" />
        <Setter Property="Margin" Value="8,4" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <StackPanel>
                        <Rectangle Height="1" Fill="{DynamicResource Brush.StrokeShadow}" />
                        <Rectangle Height="1" Fill="{DynamicResource Brush.StrokeDivider}" />
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="Separator" BasedOn="{StaticResource Menu.Separator}" />

    <!-- ============ Submenu item / header ============ -->
    <ControlTemplate x:Key="MenuItem.SubmenuTemplate" TargetType="MenuItem">
        <Grid x:Name="Root" Background="{TemplateBinding Background}" MinHeight="{TemplateBinding MinHeight}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="28" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="16" />
            </Grid.ColumnDefinitions>

            <ContentPresenter x:Name="Icon" Grid.Column="0"
                              Content="{TemplateBinding Icon}"
                              HorizontalAlignment="Center" VerticalAlignment="Center" />
            <TextBlock x:Name="Check" Grid.Column="0"
                       Text="{DynamicResource Glyph.CheckMark}"
                       FontFamily="{DynamicResource Font.Icons}" FontSize="12"
                       Foreground="{DynamicResource Brush.Accent}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="Collapsed" />

            <ContentPresenter x:Name="Header" Grid.Column="1"
                              Margin="4,0,12,0"
                              ContentSource="Header"
                              RecognizesAccessKey="True"
                              VerticalAlignment="Center"
                              TextElement.Foreground="{TemplateBinding Foreground}"
                              TextElement.FontFamily="{TemplateBinding FontFamily}"
                              TextElement.FontSize="{TemplateBinding FontSize}">
                <ContentPresenter.Resources>
                    <!-- A string header renders through the app-wide implicit TextBlock style,
                         whose Foreground would outrank the disabled colour this template sets. -->
                    <Style TargetType="TextBlock">
                        <Setter Property="TextOptions.TextFormattingMode" Value="Ideal" />
                        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
                    </Style>
                </ContentPresenter.Resources>
            </ContentPresenter>

            <TextBlock x:Name="Gesture" Grid.Column="2"
                       Text="{TemplateBinding InputGestureText}"
                       Margin="0,0,8,0"
                       VerticalAlignment="Center"
                       FontFamily="{DynamicResource Font.Small}"
                       FontSize="{DynamicResource Size.Caption}"
                       Foreground="{DynamicResource Brush.TextSecondary}" />

            <TextBlock x:Name="Chevron" Grid.Column="3"
                       Text="{DynamicResource Glyph.ChevronRight}"
                       FontFamily="{DynamicResource Font.Icons}" FontSize="10"
                       Foreground="{DynamicResource Brush.TextSecondary}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="Collapsed" />

            <Popup x:Name="PART_Popup"
                   AllowsTransparency="True"
                   Placement="Right"
                   HorizontalOffset="-4"
                   Focusable="False"
                   PopupAnimation="None"
                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}">
                <Border Background="{DynamicResource Brush.Bg2}"
                        BorderBrush="{DynamicResource Brush.StrokeDivider}"
                        BorderThickness="1"
                        CornerRadius="{DynamicResource Radius.Medium}"
                        Padding="0,4"
                        MinWidth="180"
                        SnapsToDevicePixels="True">
                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" />
                </Border>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="Role" Value="SubmenuHeader">
                <Setter TargetName="Chevron" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="Icon" Value="{x:Null}">
                <Setter TargetName="Icon" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Check" Property="Visibility" Value="Visible" />
                <Setter TargetName="Icon" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Brush.Bg3}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Header" Property="TextElement.Foreground" Value="{DynamicResource Brush.TextDisabled}" />
                <Setter TargetName="Gesture" Property="Foreground" Value="{DynamicResource Brush.TextDisabled}" />
                <Setter TargetName="Icon" Property="Opacity" Value="0.4" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- ============ Top-level item in a Menu bar ============ -->
    <ControlTemplate x:Key="MenuItem.TopLevelTemplate" TargetType="MenuItem">
        <Grid>
            <Border x:Name="Root" Background="{TemplateBinding Background}"
                    CornerRadius="{DynamicResource Radius.Small}" Padding="10,0">
                <ContentPresenter ContentSource="Header" RecognizesAccessKey="True"
                                  VerticalAlignment="Center"
                                  TextElement.Foreground="{TemplateBinding Foreground}"
                                  TextElement.FontFamily="{TemplateBinding FontFamily}"
                                  TextElement.FontSize="{TemplateBinding FontSize}" />
            </Border>
            <Popup x:Name="PART_Popup"
                   AllowsTransparency="True"
                   Placement="Bottom"
                   VerticalOffset="2"
                   Focusable="False"
                   PopupAnimation="None"
                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}">
                <Border Background="{DynamicResource Brush.Bg2}"
                        BorderBrush="{DynamicResource Brush.StrokeDivider}"
                        BorderThickness="1"
                        CornerRadius="{DynamicResource Radius.Medium}"
                        Padding="0,4"
                        MinWidth="200"
                        SnapsToDevicePixels="True">
                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" />
                </Border>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Brush.Bg2}" />
            </Trigger>
            <Trigger Property="IsSubmenuOpen" Value="True">
                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Brush.Bg3}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="MenuItem.Default" TargetType="MenuItem">
        <Setter Property="MinHeight" Value="28" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="FontFamily" Value="{DynamicResource Font.Text}" />
        <Setter Property="FontSize" Value="{DynamicResource Size.Body}" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextPrimary}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template" Value="{StaticResource MenuItem.SubmenuTemplate}" />
        <Style.Triggers>
            <Trigger Property="Role" Value="TopLevelHeader">
                <Setter Property="Template" Value="{StaticResource MenuItem.TopLevelTemplate}" />
                <Setter Property="Height" Value="28" />
            </Trigger>
            <Trigger Property="Role" Value="TopLevelItem">
                <Setter Property="Template" Value="{StaticResource MenuItem.TopLevelTemplate}" />
                <Setter Property="Height" Value="28" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="MenuItem" BasedOn="{StaticResource MenuItem.Default}" />

    <Style x:Key="ContextMenu.Default" TargetType="ContextMenu">
        <Setter Property="Background" Value="{DynamicResource Brush.Bg2}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.StrokeDivider}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="0,4" />
        <Setter Property="HasDropShadow" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{DynamicResource Radius.Medium}"
                            Padding="{TemplateBinding Padding}"
                            MinWidth="200"
                            SnapsToDevicePixels="True">
                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ContextMenu" BasedOn="{StaticResource ContextMenu.Default}" />

    <Style x:Key="Menu.Default" TargetType="Menu">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextPrimary}" />
        <Setter Property="FontFamily" Value="{DynamicResource Font.Text}" />
        <Setter Property="FontSize" Value="{DynamicResource Size.Body}" />
        <Setter Property="Height" Value="32" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Menu">
                    <Border Background="{TemplateBinding Background}">
                        <ItemsPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Menu" BasedOn="{StaticResource Menu.Default}" />

</ResourceDictionary>
