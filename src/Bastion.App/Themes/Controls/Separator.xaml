<!--
    The etched seam (UI-CONTRACT.md section 2, signature detail 1): every divider in Bastion
    is 2 px - 1 px Brush.StrokeShadow above 1 px Brush.StrokeDivider - so panels read as
    machined plates rather than as CSS rules.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="Style.Seam" TargetType="Separator">
        <Setter Property="Height" Value="2" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <StackPanel SnapsToDevicePixels="True">
                        <Rectangle Height="1" Fill="{DynamicResource Brush.StrokeShadow}" />
                        <Rectangle Height="1" Fill="{DynamicResource Brush.StrokeDivider}" />
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Style.SeamVertical" TargetType="Separator">
        <Setter Property="Width" Value="2" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <StackPanel Orientation="Horizontal" SnapsToDevicePixels="True">
                        <Rectangle Width="1" Fill="{DynamicResource Brush.StrokeShadow}" />
                        <Rectangle Width="1" Fill="{DynamicResource Brush.StrokeDivider}" />
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Separator" BasedOn="{StaticResource Style.Seam}" />

    <!-- A single hairline for the rule half of a rule-and-caps section header. -->
    <Style x:Key="Style.Hairline" TargetType="Rectangle">
        <Setter Property="Height" Value="1" />
        <Setter Property="Fill" Value="{DynamicResource Brush.StrokeDivider}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!-- Grid splitter: reads as a seam that can be grabbed. -->
    <Style x:Key="GridSplitter.Seam" TargetType="GridSplitter">
        <Setter Property="Width" Value="6" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="ShowsPreview" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource Focus.RingTight}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GridSplitter">
                    <Grid Background="Transparent">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Rectangle x:Name="Shadow" Width="1" Fill="{DynamicResource Brush.StrokeShadow}" />
                            <Rectangle x:Name="Divider" Width="1" Fill="{DynamicResource Brush.StrokeDivider}" />
                        </StackPanel>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Divider" Property="Fill" Value="{DynamicResource Brush.StrokeControlHover}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
