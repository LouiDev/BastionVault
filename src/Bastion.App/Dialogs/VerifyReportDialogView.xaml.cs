using System.Windows.Controls;

namespace Bastion.App.Dialogs;

/// <summary>View for the matching view model; it has no logic of its own.</summary>
public partial class VerifyReportDialogView : UserControl
{
    /// <summary>Creates the view.</summary>
    public VerifyReportDialogView() => InitializeComponent();
}
